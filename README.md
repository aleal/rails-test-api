# README

## Setup

First of all, you should change postgresql user at config/database.yml. Or, if you will, add user aleal to your database. Then:
```
rails db:create
rails db:migrate
rails test
rails s
```

